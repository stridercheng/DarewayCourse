<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="cn.com.dareway.sample.MainActivity">

    <FrameLayout
        android:id="@+id/fl_container"
        android:layout_above="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
    <ImageView
        android:id="@+id/divider"
        android:layout_above="@+id/rg_bottom"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/colorPrimary"/>
    <RadioGroup
        android:id="@+id/rg_bottom"
        android:paddingTop="5dp"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="54dp">

        <RadioButton
            android:id="@+id/rb1"
            android:text="消息"
            android:gravity="center"
            android:button="@null"
            android:drawableTop="@drawable/message_rb"
            android:textColor="@drawable/txt_selector"
            android:layout_width="0px"
            android:layout_weight="1"
            android:layout_height="match_parent" />

        <RadioButton
            android:id="@+id/rb2"
            android:gravity="center"
            android:text="联系人"
            android:button="@null"
            android:drawableTop="@drawable/contact_rb"
            android:textColor="@drawable/txt_selector"
            android:layout_width="0px"
            android:layout_weight="1"
            android:layout_height="match_parent" />

        <RadioButton
            android:id="@+id/rb3"
            android:gravity="center"
            android:text="空间"
            android:button="@null"
            android:drawableTop="@drawable/space_rb"
            android:textColor="@drawable/txt_selector"
            android:layout_width="0px"
            android:layout_weight="1"
            android:layout_height="match_parent" />
    </RadioGroup>
</RelativeLayout>
